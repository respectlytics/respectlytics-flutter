name: respectlytics_flutter
description: Official Respectlytics SDK for Flutter - Privacy-first analytics with session-based tracking. No device identifiers, GDPR/ePrivacy compliant, no consent required.
version: 2.0.1
homepage: https://respectlytics.com
repository: https://github.com/respectlytics/respectlytics-flutter
issue_tracker: https://github.com/respectlytics/respectlytics-flutter/issues

topics:
  - analytics
  - privacy
  - tracking
  - flutter-plugin
  - gdpr

platforms:
  android:
  ios:
  linux:
  macos:
  windows:

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3
  http: ^1.6.0
  uuid: ^4.5.2
  connectivity_plus: ^7.0.0
  package_info_plus: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
